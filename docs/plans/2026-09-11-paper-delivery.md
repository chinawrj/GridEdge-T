# 2026-09-07 至 09-11 模拟盘上线交付约定

所有时间为 Asia/Shanghai；交易窗口以项目受审 A 股日历为准。用户要求本周得到真正上线运行的模拟仓位系统。本文件规定交付节奏，不修改 GOAL.md 的策略、数据或资金语义。目标截止为 2026-09-11 17:10，绝不自动顺延或降低验收标准。

## 范围和验收

继续正式 run ths-002256-20260819-grid15-opening-v1、002256.SZ、Q=5500、grid_ratio=0.015，保留既有 27500 股和全部账本/outbox。不得重复初始建仓或用内部回测替代同花顺模拟账户。冻结新功能、UI 美化和无关重构。

交付须证明：
1. 已审安装版本与账本 effective 身份一致；当前行情带可信来源/连续性/完成边界并获精确 PostgreSQL COMMITTED ACK；正常启动 45 秒内首条 observation/ACK。
2. 09:25 就绪、09:35 首根完成 bar；交易时段所有按合同应处理的 bar 均有按当时 mode 核验的 RECEIVED→DECISIONS_COMMITTED→PROCESSED，策略实际评估。零遗漏不能由事后 READ_ONLY 补齐冒充。
3. 周四 09-10、周五 09-11 两个完整交易日持续运行并逐日收盘对账；缺失会话、漏 bar、人工反复救火均判该日失败。完整市场归档包括最后桶，但 GOAL.md 对新委托的 11:25/14:55 截止规则不变。
4. 现有模拟账户仓位、现金、费用口径、可卖量、订单终态、账本/outbox 一致；明确区分核心 Paper 保守费用与远端实际费用不可观测部分。
5. 本周自然策略机会产生的合法新 OrderIntent 必须唯一提交到同花顺模拟账户并对账至终态。记录实际成交，而非仅点击成功；若市场没有可执行机会，只能报告“运行验收通过、持续成交未验证”，不得冒称已证明网格买卖闭环，不为赶期限强制下单、改变参数或重复建仓。
6. app-independent supervisor/guard 持续拥有运行服务。普通故障按原 5 分钟预算恢复，代码缺陷按 15 分钟预算缓解；独立夹具验证重启/断连恢复，不对正式账户注入故障。所有开放的安全/可用性缺陷必须有真实结论，不能用单根 RUNNING 或测试通过替代完整验收。

## 两个 timer 与唯一 owner

- 运维 timer：更新已有 automation-2，在既有运维任务里每个工作日 08:00 至 15:30 每半小时运行。08:00 提前发现人工在场边界；09:00 巡检未就绪则用 <=60 秒有界检查留到 09:25；09:30 留到 09:35 核实首 bar；12:30 核验独立 supervisor 将执行 12:55 预检，13:00 留到 13:05；15:00 核验收盘，15:30 完成日账。正常无变化不重复通知。
- 交付 timer：当前任务 heartbeat，本周工作日 08:10 至 22:10 每小时唤醒。每轮推进当前最大阻塞，能继续的工作持续做，不以一个小时为结束预算。下一次 timer 是掉线后的续接，不能是“等下一轮再修”的理由。已有进程/审查未结束则跟踪同一 handle，不重复跑相同试验。
- 原 goal 的每日 15:20 独立盘后修复 timer 暂停，其职责由交付 timer 的 15:10 后轮次接管，避免多个任务同时改同一发布路径。周五最终报告后交付 timer 暂停；运维 timer 继续服务。逾期时也先保存未完成项与结论再暂停本周 timer，保留运维，不能暗中延长期限。
- 运行控制始终只有已有 guard。交付任务先读运维任务状态及 supervisor/维护锁；候选只在隔离目录和隔离 broker/topic 验证。市场时段运维 owner 优先，交付任务不得并发操控正式 Chrome、Android、worker 或安装。发布必须在既有维护/协调机制证明独占后进行，否则继续隔离工作。
- Codex 本地 timer 需要电脑开启且桌面应用运行；独立运行守护不能代替 AI 开发任务调度。每轮先检查错过的窗口，不能假设漏掉的 timer 已执行。
- 09-08调度更正：有截止日期的交付规则已改为显式UTC起点和等价北京时间小时，保持本周08:10–22:10及周五截止。每次规则更改须运行 `node docs/plans/verify-delivery-schedule.cjs` 或经检查更新的等价边界用例，并只读核验实际next_run_at；不能只回读TOML。当前应用有0–119秒抖动，名义08:10的实际首轮是08:11:52。09-11的16:10轮次必须在同轮完成最终审计并于17:10前暂停交付timer，不能等待抖动后的17:10轮次才开始收尾。时段外异常唤醒只做短时健康检查，有事故才恢复；无变化不扩写代码、不重复测试或通知。

## 固定里程碑

| 截止 | 必须产生的结果 | 未达到时的处置 |
| --- | --- | --- |
| 09-07 22:10 启动，09-08 08:10 交接 | 完成现有能力与数据源只读盘点；列出候选访问方式、实际样本、完成边界证据、权限与接入代价；把 CAPTCHA 与代码工作分开；一页阻塞表 | 缺少访问凭据/正常 GUI 用户操作就一次性给出确切动作，继续所有独立工程工作 |
| 09-08 12:10 | 锁定能实际读取、可审计且可验证完成边界的行情路线，完成决定性最小 probe；不能只找到协议文档 | 原 DOM 无法证明完整性就研究并验证独立获准来源。需要新费用、账户或权限则给出具体方案请用户决定；无可用源则此刻明确报告本周交付风险，不能继续把重载当根治 |
| 09-08 22:10 | 最小数据/运行修复实现；可复现独立回归由失败到通过；隔离行情→ACK→策略→outbox 链路验证；冻结剩余清单 | 保留健康正式版本，停止无关修改；更新真实关键路径，不重复已绿测试充数 |
| 09-09 12:10 | 完成两轮独立完整 replay/recovery、规定审查、格式/Clippy/全测试/端到端 replay；实际只读预检和受控发布就绪 | 未完成则明确两日验收窗口已受威胁；继续最小修复，不放宽门禁 |
| 09-09 18:10 | 完成允许盘后进行的构建/签名/同 run 升级或明确的次日现场激活，发布回滚材料和周四预检 | live-only 激活必须由周四 09:00 运维 owner 实际执行并于09:25通过；外部阻塞不伪称部署完成 |
| 09-10、09-11 09:25 / 09:35 | 每日预检通过 / 首 bar 全链 RUNNING；按交易时段持续验收 | 立即 P0 和同轮恢复；错过的事实保留，不能重放订单掩盖失败 |
| 09-10 16:10 | 第一天完整运行账、缺失 bar、故障预算、新委托/成交及现金仓位对账 | 当晚仅修影响次日的缺陷；影响验收身份的发布需重新说明证据适用范围 |
| 09-11 16:10 开始，17:10 截止 | 最终报告：已上线并满足验收 / 已运行但成交未验证 / 未交付，附证据、访问入口、安装身份、恢复入口、日账与遗留问题 | 未交付必须直说及说明最早可验证的缺口，不以“做了很多工作”收口 |

## 每轮工作纪律

先读本文件、AGENTS.md、GOAL.md、自身 automation memory、当日 incident 和实际 supervisor/账本/outbox；历史摘要不作为现状。状态记录维护在 docs/plans/2026-09-11-paper-delivery-status.md。

每轮选择一个阻塞，写明独立验证方法并完成有结果的下一步。记录证据路径与时间、改变了什么、下一动作、活进程/任务 handle。单纯重读状态不算工程进展；同一方案累计两小时或两轮无新证据时必须重新评估路线，不能用换版本号或重跑测试重置时钟。评估可以得出外部必需条件，但必须用已执行的 probe 证明。

独立来源研究须检查 GOAL.md 准入要求；网络可读不等于授权投产。不得绕过 CAPTCHA、URL 安全策略或新增费用/账户；不得把时钟、HTTP Date、最新价或后补历史当完整 OHLC。新来源先独立身份、隔离发布路径、审查和来源准入；策略权利/不亏语义变更仍须固定数学及量化审查席批准。

每日 08:10 给出当天关键目标，18:10 给出结果/风险/下一截止；里程碑失败、P0、需要用户动作时及时通知。没有状态变化时不重复催相同人工动作。交付 timer 的这些报告是用户要求的交付进度；运维健康静默。

## 已核实起点（2026-09-07）

正式扩展0.6.51，0.6.53候选已局部审查但尚未激活；CAPTCHA/GUI重载有用户在场边界。末桶完成与无成交时段覆盖证明仍是开放缺陷，不能声称“盘后全部闭环”。最后账本/outbox1957/1957、最新13:50 bar为READ_ONLY、cash103418.530、position/sellable27500、unresolved0。8月19日初始建仓的FILLED事实为27500股、11条明细、成交金额92754；不是本周新交易。21:34已直接核验supervisor PID22058与PID1 caffeinate1673存活；PID不是未来健康证据。

详细历史已审身份与修复规程归档在 2026-09-07-automation-legacy-baseline.md；后续必须读取真正的 installed/effective 与追加式 binding。OpenAI Docs核对的本地调度限制见 https://learn.chatgpt.com/docs/automations?surface=app 。
