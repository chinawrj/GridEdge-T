# 行情源路线决定：实际探测与待验边界

09-10 10:24不同方向合同筛选：读取米筐官方[实时推送说明](https://www.ricequant.com/doc/rqdata/python/generic-api)，其明确披露实时分钟线由盘中已收tick聚合，迟到tick可能不纳入，盘后重新清洗聚合且以历史线为准。因此“实时分钟推送天然等于完整最终OHLC”这一具体接入假设不成立；未测试其其他数据产品，也不否定可另行取得的修订/完整性合同。掘金官方[on_bar说明](https://myquant.cn/docs/python/python_data_event)提供固定周期服务端推送及bob/eob，但所读页面没有迟到修订或零成交末桶完成保证，暂列合同未证明而非判定数据错误。本次只是公开文档筛选，没有行情样本、账户、费用、安装、发布或准入。下一步只能验证不同产品的明确完整性/修订合同与实际权限，不能重跑这些文档已否定的默认假设。

09-10 09:17新方向源码审计：Longport SDK 的 confirmed K线不是原生交易所完成回执。固定上游commit72e9be585d2724358ddaf7d6afbb64bb9e01205d，普通股票分钟线由trade本地聚合；下一桶trade触发 `AppendNew.confirmed=Some(prev)`，生成的K线事件sequence=0。故“只用is_confirmed修复quiet-tape/末桶”明确淘汰，不因此开户/付费。结论仅覆盖该SDK路径，不否定其他产品或未经测试的授权feed。当前工具无可调用Longbridge连接器，PATH无CLI、标准配置目录缺席；plugin-management的search/suggest工具未暴露，未安装或读取凭据。证据evidence/2026-09-10-longport-confirmed-source-audit.json，含固定源码URL及SHA。下一不同方向须有独立的序列连续性、迟到/修订、零成交和时段结束合同，不能再次把客户端下一笔确认当finality。

9月9日live窗口结论（9月10日01:30记录）：既有observer只获得1成功响应和1超时，11:57退出。09:52样本精确审计证明分钟行和顶层累计量额不一致：多41手/13774元，不能把9月8日盘后相等推广为盘中完整性。没有足够连续样本研究修订，也没有OHLC/完成标记。此具体“公开分钟数组+顶层总量直接证明完成”路径不准入；下一方向须原生OHLC/逐笔及完整性协议，不能重采相同静态样本、猜参数或按等待时长签发finality。证据evidence/2026-09-09-source-window-failed.json。

## 09-08 19:14：金额精度审计纠正

18:32 recorder 的 `serde_json::Value` 默认先把小数解析为浮点，再转 Decimal；其 inspection 不能作为通用精确金额证明。原始响应字节与 SHA 不受影响。新增 `docs/plans/audit-szse-amounts.py` 先验 receipt SHA，直接从原始 JSON 数字得到 Decimal，再按十进制位转换为整数分。高精度反例 `9007199254740991.01` 证明旧路径丢失分位，新审计保留 `900719925474099101` 分；重复键、非有限数和不足一分的输入均拒绝，三项测试通过。

既有原始样本重算后，常规行与累计值均为 `12542183319` 分、`371092` 手。明日采样进程继续保留原始数据，金额结论必须使用本审计入口；recorder 的浮点 inspection 只作诊断。它仍不提供 OHLC 或完成性，未获得独立审查/正式准入。证据：`evidence/2026-09-08-szse-exact-amount-audit.json`。

2026-09-07 22:26 Asia/Shanghai。状态：研究中，**没有新来源获准投产**。本轮未改正式 Chrome、Android、MQTT、行情账本、安装文件或订单。

## 已取得的真实样本

独立证据目录：`/Users/rjwang/Library/Application Support/GridEdge-T/runtime/source-probes/20260907-2213`。

| 来源 | 实际读取结果 | 尚未证明 |
| --- | --- | --- |
| 现有 BaoStock TCP 客户端 | 22:13 读取 002256.SZ，09-04/09-07 五分钟线共 96 根，每日 48 根；包含 11:30/15:00 标签；输出、原始数据和质量报告已保留 | 这是盘后历史结果，不证明盘中及时性或最后桶完成 SLA |
| 新浪官方股票页加载的行情 SDK 端点 | 从公开页面、SDK、datas/k.js 逐级确认 URL，实际取得 1023 根五分钟线，09-07 有 48 根，原始 JSONP 已保留 | 返回无 symbol 回显、amount、complete/revision 字段；时间标签本身不证明完成；未审查准入 |
| Tushare rt_min | 已读官方实时分钟文档，尚无实际已配置凭据 probe | 独立权限，官方权限页列 1000 元/月；未购买、未新增账户；接口文档未给完成标志 |
| 同花顺 QuantAPI | 已读官方 FAQ，HTTP 可在 Mac 使用，分钟 K 有采样口径说明 | 需要独立账户/授权；模拟交易 App 登录不能推定拥有 API 权限；未取得样本 |

凭据检查仅限本项目配置/部署目录及环境变量名称，未发现上述商业来源配置；不代表用户在其他地方没有账户。不要打印凭据。

官方资料：[BaoStock](https://www.baostock.com/)、[新浪标的页](https://finance.sina.com.cn/realstock/company/sz002256/nc.shtml)、[Tushare 实时分钟](https://tushare.pro/document/2?doc_id=374)、[Tushare 权限](https://tushare.pro/document/1?doc_id=290)、[QuantAPI FAQ](https://quantapi.10jqka.com.cn/gwstatic/static/ds_web/quantapi-web/help-center/faq.html)。BaoStock 官方站实际帮助文档响应保存在证据目录。

## 独立对照：不能仅按时间拼接换源

用 Decimal 对齐两源的 96 个时间标签：55 根 OHLCV 全同，41 根至少一个字段不同。open/close/volume/high/low 分别有 30/3/12/3/1 根不同。

每日总量均相同：09-04 为 31137700 股，09-07 为 31745087 股。但 09-07 最低价 BaoStock=3.29、新浪=3.28；相同总量不足以认证价格。可能涉及集合竞价、分桶或采样差异，目前未确定根因，不判定任一来源错误。

`cross-source-comparison.json` 保存差异计数、样例和原始文件 SHA。`baostock-quality.json` 是原始报告的原样复制，其中路径仍指向首次临时输出；权威保留字节以本目录和记录 SHA 为准。

## 下一项决定性观测

新 `src/bin/gridedge_source_observation_probe.rs` 是独立只读研究程序：不用项目行情发布/执行模块，不连接 MQTT、PostgreSQL、账本或模拟账户，只读公共 HTTPS 响应并写独占研究目录。源响应只解析 JSON，绝不执行 JavaScript。

计划窗口：09-08 09:29–11:32，每次响应后间隔 30 秒，单次请求最多 20 秒。以独立 tmux 中的冻结副本执行，不依赖下一次 AI timer。实际 PID、SHA、等待心跳须记录到交付 status 后才算已经安排。程序明确输出 `admitted=false`、`source_completeness_proven=false`，包括未来标签也只存研究证据。

观察目标：第一根 09:35 的出现延迟、形成中标签、后续修改、无成交时间段和 11:30 最后桶的更新行为。多次相同响应只能提供经验性稳定性证据，不能自动补出来源合同中缺失的完整性承诺。若不能满足 GOAL.md 准入，则不能发布。

明早 08:10 检查观测进程、正式运维状态并交接；11:32 后分析保存的响应；12:10 必须决定可审计路线或给出具体外部条件和本周交付风险。不得无限扩展供应商调研或重新进入 CAPTCHA 等待循环。

## 本轮验证范围

观测程序 7 项单测及严格全仓 Clippy 已通过。完整格式/测试门禁另见交付 status。现有 release 的隔离样例 replay 已完成：233 个事件、0 重复、无开放委托；只验证回放，不是远端成交，也不是新源两轮实时恢复验收。

## 23:11 续接：预先准备离线分析

新增独立 Rust 二进制 `gridedge_source_observation_analyze`，不修改正在等待的冻结 recorder。它只读 identity、连续编号 receipt 和原始响应；验证 URL/研究标识、fixture 属性、文件名绑定、SHA、时间顺序和 Decimal OHLC/整数成交量。编号缺口、证据篡改、身份漂移直接失败；网络/解析失败保留在报告中，不静默丢弃。

明早在窗口结束后执行（盘中也可分析已落盘前缀；读到正在写入的 receipt 时失败后有界重读）：

```sh
target/debug/gridedge_source_observation_analyze \
  --input-dir '/Users/rjwang/Library/Application Support/GridEdge-T/runtime/source-probes/20260908-sina-observation' \
  --date 2026-09-08
```

报告逐标签输出首次/末次读取、首次读取相对标签的毫秒差、标签后首次观察、修改字段及其是否发生于标签之后；另列连续回执最大间隔、读取失败、消失标签和48个标准交易时段标签中的缺项。价格仅精度格式不同不算修订。日期参数是研究筛选，不认证交易日历。

使用限制：

- 报告是文件目录中已落盘回执前缀的分析，**不证明观测窗口已结束**。须同时检查 recorder 的 identity 起止时间、实时进程和 status；失败/提前停止不能当完整窗口。
- 未到期标签也会列为 missing，不能把午前尚无午后标签算故障。
- 仅一次盘后响应的最大间隔为0、修订数为0，只说明没有可比较的第二次读取，不能说明实时、稳定或低延迟。现有 smoke 正是这种情形。
- `first_observation_at_or_after_label_ms` 是采集端观察时刻，不是来源公布完成时刻。标签后修改也不等于已确认的交易所更正。
- 整个报告始终 `admitted=false` / `source_completeness_proven=false`。即使明早48标签或24上午标签都存在，也不能自动通过新源准入。

实际22:25网络样本已成功经过离线工具：09-07 的48个标签均能解析，哈希绑定通过，无解析失败；输出保存于证据目录 `analyzer-live-smoke-report.json`。下一决定仍是明日12:10，分析工具不是替代所缺来源完整性合同的新门槛或延期理由。

## 09-08 08:13:39 盘前可达性证据

使用相同冻结recorder做一次有界网络预检，已正常退出，目录安装根 `runtime/source-probes/20260908-preopen-0814`（目录名不是取证时间，receipt精确时间为08:13:39）。1023行、最后标签09-07 15:00，原始SHA仍 `2d797ccf4fb397ff91028902c523c99312f217faa149b677dd2c00ea7e738e3d`。这证明当前可读取及隔夜样本字节相同，不能证明盘中实时、标的回显、最终完成或换源一致性。

09:29–11:32既有观测进程PID92463及父tmux92462已连续过夜，identity和二进制哈希重新核验。一次性预检使用独立目录、不并入正式观测序列，不改变无MQTT/无交易权限的隔离边界。12:10决定点保持；当前不需要新增分析功能来等待市场开盘。

## 09-08 10:18：盘中决定性反例，尚不准入

冻结分析器对09:29:10至10:14:44的91个连续回执分析成功，0采集失败，最大回执间隔31.392秒。完整前缀报告保存在 `docs/plans/evidence/2026-09-08-sina-morning-prefix.json`（SHA1eadd0da…），原始响应及每条SHA仍在既有独占采集目录。

新浪9个标签中有8个已越过标签时间；这8根均观察到标签后字段变化。这并非已证明的交易所“事后更正”，只描述接收端的观测行为。以下反例足以拒绝单纯按标签时间/第一次标签后读取就认定完成：

- 09:35标签在09:29:10已出现，距标签仍349.531秒；09:35:14首次标签后观察还有close及volume变化。
- 09:55标签在09:55:00.726首次标签后读取后，09:55:31.069成交量仍变化。
- 10:00标签在10:00:04.325读取后，10:00:34.621的close和volume仍改变。两条原始响应分别为`response-00061.jsonp`/`response-00062.jsonp`，SHA6f252e9a…/e5c2ef14…。不能将10:00:04的版本作为已完成bar送入策略。

本结果不证明固定等待30/60秒或连续两个相同响应就足够，也不否定供应商可能有其他已完成接口。当前端点没有完成/修订标志、symbol回显或amount；先保持研究隔离，继续原进程至11:32观察上午末桶，不能更改策略时间或假造完成证明。

10:16:26已对现有BaoStock Rust客户端执行今天5分钟查询，返回EOF解析错误而无可用bar；10:16:45同客户端昨日控制成功取得48根。证据 `docs/plans/evidence/2026-09-08-baostock-intraday-probe.json` 及同目录control原始/转换CSV。该失败只能说明当前路径尚未得到今天可用行情，不能直接归因为供应商没有今天数据，更不能当0成交。

官方文档复核（本轮）仍不足以把“换付费API”当已完成方案：[Tushare rt_min](https://tushare.pro/document/2?doc_id=374)公开字段有证券代码、时间和OHLCV/amount，但未列完成/修订标志；[QuantAPI FAQ](https://quantapi.10jqka.com.cn/gwstatic/static/ds_web/quantapi-web/help-center/faq.html)说明分钟线可能由快照合成、不同采样源可能不同，实时逐笔需其datafeed产品。因此实测与准确产品权限/分桶定义仍是准入前提，不以购买替代审查。

已非阻塞询问用户是否有可在本机安全配置的实时分钟接口账号；不请求聊天发送凭据，不新增费用/账户。12:10仍须作明确路线或外部条件/本周风险结论，不等待用户输入才继续现有研究。


## 09-08 11:39：完整上午窗口路线决定（提前完成12:10决定点）

结论：**现有新浪端点保留为研究来源，不准入正式交易；BaoStock当前路径未取得可用盘中证据；当前没有可签发投产的新行情源。** 本周稳定无人值守交付处于阻塞风险，不能承诺以候选代码或固定延迟替代来源证明。

既有唯一recorder于11:32:13完成计划窗口，独立一次性分析于11:32:28完成；两进程正常退出，未重复启动。完整报告 `evidence/2026-09-08-sina-morning-complete.json` SHA `5c0f98038a7f374705fd4c479a43daf5512ba34f832dc0128aaa4f807b360f83`；243个连续receipt（0..242），首请求09:29:10.210876，末接收11:31:43.206183。ordered SHA `fafc9ba8ff000e5b65497eafd1311000b81387b59e6b8057a8e69443d2942b4f`。最大接收间隔31.392839秒（Rust报告以整数毫秒截断为31392），无采集失败、消失标签或意外标签。全部24个上午标签存在；报告列出的24个午后missing标签尚未到期，不能算故障。

独立审查从原始JSONP/receipt重算全部哈希、序列、时间和Decimal数值，与报告一致。24个标签均在标签结束后仍观测到字段变化，共30次。最大最后变化观察延迟为11:25标签的38.301342秒。此为接收端观察时间，不能宣称交易所在该时刻更正行情。

| 标签 | 原始反例 | 结论范围 |
| --- | --- | --- |
| 11:25 | seq228成交量684300；seq229于11:25:07.943507为688900；seq230于11:25:38.301342为693800，OHLC不变 | 第一次标签后响应仍不是最后观测版本 |
| 11:30 | seq238成交量454000；seq239于11:30:12.020051为467200；seq240..242至11:31:43.206183的完整raw字节与239相同 | 观察到稳定尾部，但没有来源原生完成性证明 |

末四个完整响应SHA `4013b32cb3acb60b5822b39c42b3516f2f0ec9b24a26f22251698cad2a2b6b44`。稳定次数、HTTP时间和经验性等待均不能补出接口缺失的finality/完整性合同。不得将研究bar发布到正式订阅主题，不得跨来源按时间拼接，不得放松60秒覆盖门槛。

下一条可执行路线是：取得本机可使用、权限明确的行情来源及其证券身份绑定、分桶/完成/修订语义证据，再按既定两轮完整隔离replay/recovery及真实只读preflight准入。来源可以是当前供应商的合格接口，也可以是已授权其他接口；购买本身不等于准入。现有公开端点尚不能满足该条件。用户此前收到的本机安全配置询问仍有效，不在聊天索要密钥，不新增费用/账户，不重复广泛供应商调研。

当前正式恢复另有人工边界：11:37重新只读确认唯一受审Eastmoney页仍有滑块验证，证券头字段缺失，0.6.53正式激活仍未证明。需由用户按此前已发送说明完成页面验证/正常GUI加载受审扩展；该动作完成也不能自动关闭quiet-tape/最后桶完整性缺陷。原人工请求不重复发送或绕过。

午后保持现有受审guard/worker及已发布supervisor运行。11:37:52实际heartbeat、PID/出生时间和完整安装manifest均通过；精确安装supervisor的纯时间函数确认12:55进入PREFLIGHT、13:00进入TRADING监督窗口。它只负责既定依赖检查/恢复，订单仍必须经过新鲜连续行情、committed ACK和终端账户门禁。没有新鲜合格行情时继续READ_ONLY。此守护存活是执行所有权证据，不是下午将恢复RUNNING的保证。

耐久证据：安装根 `runtime/e2e-audits/20260908-recovery-owner/completed-source-research`，包含原始路径及逐文件SHA清单、完成状态、完整分析和独立审查。本次只完成研究与路线决定，Goal和正式P0均未关闭。

## 09-08 15:41：现有东方财富聚合快照不能补齐完整性

盘后只读检查了受审页面已经加载的同源聚合快照接口。实际样本对`002256/兆新股份`回显身份，并返回累计量`f47=371092`、累计额`f48=125421833.19`和交易时间`f86=2026-09-08 15:34:00 Asia/Shanghai`。这证明该快照存在可用的聚合字段，不是正式准入。

页面当前逐笔历史请求只取`f51..f55`，实际展示时间、价格、数量和方向；没有逐笔精确成交额，也没有来源原生complete/revision标志。因此不能把逐笔历史同时与累计量和累计额做精确全历史核对，也不能用较晚的源时间推导quiet-tape或末桶finality。按照既有事故完成条件，该路线明确拒绝；未实现聚合水位、bar或正式发布。

证据`evidence/2026-09-08-eastmoney-aggregate-probe.json`，SHA-256 `3ab03e6628198f41c25f2c0cc06aed8f327003c34439075c5c47631c0acd238c`。下一步不是重复抓取相同端点，而是取得带精确amount的完整历史以及有文档的分桶/完成/修订语义，再进入隔离采集、ACK、replay和独立审查。

## 09-08 18:32：深交所公开分钟快照形成新候选，但尚不能构造正式五分钟 OHLC

本轮没有重复前述供应商端点，而是从深交所官方 Quotes Lookup 页面及其实际加载脚本定位到公开 `getTimeData` 路线，并实现了独立只读 recorder `src/bin/gridedge_szse_source_observation_probe.rs`。程序固定证券身份、限制响应尺寸和 HTTPS、用 Decimal 解析金额、逐行检查时序，只写全新研究目录；它不导入行情发布/执行模块，不连接 MQTT、PostgreSQL、账本或账户，并固定输出 `admitted=false` / `formal_publication=false`。

18:32:15 的真实样本回显 `002256/兆新股份`、源日期和 `marketTime=2026-09-08 15:30:00`。官方页面脚本把七列明确显示为 time、Last、Avg.、Change、% Change、Volume、Amount。返回 241 条常规时段分钟行和 25 条盘后行；常规行合计 `371092` 手、`125421833.19` 元，与响应顶层累计量/额逐 Decimal 完全相等。全部行多出的 `221` 手、`74919.00` 元恰来自 15:06–15:30 的盘后部分，未混入常规时段累计值。这首次证明了无需新账户/凭据、具备证券身份和逐分钟精确 amount、且能对常规时段量额作同源守恒检查的官方候选路径。

它仍未获准投产。分钟行只有最新价和均价，不含 open/high/low；不能用分钟最新价伪造项目所需五分钟 OHLC。响应也没有源序列、complete/final 标志或修订语义；本次盘后守恒不证明盘中及时性、桶完成或 quiet-tape。下一项决定性证据改为在下一有效盘中窗口隔离观察同一端点的滚动修订，并继续在深交所官方产品/接口范围内寻找可获得真实 OHLC 或逐笔、且有完成/修订合同的路径。候选在此前不得进入正式 topic、PG 或策略。

七项独立 parser/隔离写入回归通过，真实 once probe 正常退出；格式、严格 Clippy、全仓 503 项测试、配置校验及 233 事件隔离 replay 也全部通过（2 项真实资金 smoke 按设计忽略）。证据 `evidence/2026-09-08-szse-public-minute-probe.json`，SHA-256 `356aa9096a7de5c80425348f7f75c6012e1f341d4814d094a6268f8ed9a0d37c`；原始响应和不可覆盖回执位于 `/Users/rjwang/Library/Application Support/GridEdge-T/runtime/source-probes/20260908-szse-public-1829-once`。本结果缩小了“精确 amount + 身份”缺口，但没有关闭正式来源准入或 09-08 22:10 最小实现风险。

下一盘中观测不是仅留在timer说明中。18:47 已启动app-independent tmux `source_szse_20260909` / PID38883，固定程序SHA `96c5ce56264e9abac36c180d9ecd1e60ec66a510cde8c7e9c82fea863947d5fe`，实际状态为 `WAITING_FOR_RESEARCH_WINDOW`；它将在09-09 09:29–11:32每30秒只读同一官方端点，写全新目录 `runtime/source-probes/20260909-szse-public-live`。该进程没有MQTT、PG、账本、账户或订单访问，不能匹配正式subscriber；结果仍须独立分析/审查，不能自动准入。执行所有权证据见 `evidence/2026-09-08-szse-next-session-observer.json`，SHA-256 `7d3ed8422c00e5566160b0379e4555caeda269dd2766516698e0bcc33305cbec`。
